# ChronoReplay — Zero-Dependency Verification Report

**Python Version:** Python 3.10+ 
**Third-Party Runtime Dependencies:** 0
**Third-Party Build / Test Dependencies:** 0

## 1. Automated Test Suite Execution

```bash
python3 -m unittest discover -s tests -p "test_*.py"
```

**Execution Output:**
```text
......................................................................................................................................................
----------------------------------------------------------------------
Ran 150 tests in 2.694s

OK
```

## 2. Standard-Library Modules Used in Shipped Code

```text
copy
dataclasses
datetime
difflib
hashlib
json
os
pathlib
sqlite3
sys
time
tkinter
typing
uuid
```

## 3. Manifest Verification
- `requirements.txt`: 0 third-party entries (empty / comment only)
- `.zero-dep.toml`: Configured for Track F with 0 external dependencies
- All import statements throughout `src/` and `artifacts/` use strictly Python standard library.

## 4. Standalone Single-File Reproducible Artifact & Receipt

The standalone self-contained artifact `artifacts/chronoreplay-single.py` is deterministically generated from source and runs independently with 0 third-party packages.

- **Artifact Path:** `artifacts/chronoreplay-single.py`
- **Build Script:** `build.py`
- **Reproducibility Test:** PASS (Identical SHA-256 across dual build passes)
- **SHA-256 Checksum:** ` 739F551DE411CB063A8ED46E82CD2A9C9E941099C1917AA1DA620A61F3A1CAD9`

### Verification Commands for Evaluators:
```bash
# 1. Verify build reproducibility
python3 build.py

# 2. Run standalone CLI self-test
python3 artifacts/chronoreplay-single.py --cli

# 3. Run entire application from single-file artifact
python3 artifacts/chronoreplay-single.py
```


